# Consistency-Correction Loop

작품: `{{title}}` (`{{work_slug}}`)
Run: `{{run_id}}`

## 목적

정합성 평가와 교정을 한 번으로 끝내지 않고, 만족 기준을 통과할 때까지 반복합니다.

반복 순서:

1. 정합성 평가
2. 교정/편집 batch 작성
3. 원문형 기호 적용 MD 검수본 생성
4. 후보본 적용과 diff 확인
5. 정합성 재평가
6. 해결/회귀/신규 문제 분리
7. 계속/중단 판정

## 만족 기준

- P0/P1 정합성 이슈가 모두 해결되었거나 작가 판단 보류로 분리됨.
- 중복 회차는 정본 선택, 삭제, 이식 후보가 분리되어 있음.
- 회차별 공백 제외 {{minimum_chapter_chars_no_space}}자 기준을 통과하거나, 미달 사유가 결락/분할/작가 판단으로 명시됨.
- 문맥형 오타와 패턴형 오타가 분리되어 있음.
- `changes.json` 검증, 후보본 적용, diff 검토가 완료됨.
- 각 교정 batch의 원문형 기호 적용본이 `human-facing/*_marked_manuscript.md`에 생성됨. 한글 검토가 필요하면 HWPX도 추가 생성됨.
- 재평가에서 새 P0/P1 회귀가 없음.
- 최종 개선 보고서에 before/after 근거를 쓸 수 있음.

## Loop Summary

| Iteration | 기준 원고 | 정합성 평가 결과 | 교정 batch | 재평가 결과 | 판정 | 다음 액션 |
|---|---|---|---|---|---|---|

## Iteration 기록 양식

```markdown
## Iteration 01

- 기준 원고:
- 평가 범위:
- 평가 결과:
  - P0:
  - P1:
  - P2:
  - 보류:
- 교정 batch:
- 적용 변경:
  - `ⓐ`:
  - `ⓐⓐ`:
  - 삭제:
  - 추가/브리지:
- 후보본/diff:
- 기호 적용 MD 검수본:
- 기호 적용 HWPX:
- 재평가 범위:
- 해결된 항목:
- 새로 생긴 항목:
- 회귀 항목:
- 잔여 리스크:
- 판정: 계속 / 조건부 계속 / 중단 가능
- 다음 액션:
```

## 최종 개선 보고서 재료

| 축 | Before | After | 개선 근거 | 잔여 리스크 |
|---|---|---|---|---|
| 정합성 |  |  |  |  |
| 중복/정본 |  |  |  |  |
| 회차 분량 |  |  |  |  |
| 문맥형 오타 |  |  |  |  |
| 문장 호흡/AI 티 |  |  |  |  |
| 회차 경계 브리지 |  |  |  |  |

## 중단 판정

- 최종 판정: 작성 필요
- 중단/계속 이유:
- 최종 보고서로 넘길 핵심 개선:
- 다음 작업자에게 남길 보류:
