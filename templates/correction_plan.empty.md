# Correction Plan

작품: `{{title}}` (`{{work_slug}}`)
Run: `{{run_id}}`

## 진행 상태

- 상태: 작성 필요
- 기준: 정합성 리포트와 편집자 모드 판단을 교정 batch로 구조화합니다.
- 원칙: 정합성 평가 없이 교정 batch를 만들지 않고, 교정 후에는 반드시 재평가합니다.

## 교정 batch

| Batch | 기준 평가 | 적용 범위 | 변경 수 | 주요 목적 | 검증 상태 | 다음 재평가 |
|---|---|---|---:|---|---|---|

## 변경 분류

| 분류 | 변경 기준 | marker | 승인 기준 |
|---|---|---|---|
| 확정 교정 | 의미와 말투를 거의 건드리지 않는 오류 | `ⓐ` | 문맥 근거와 95% 이상 확신 |
| 적극 편집 | 문장 호흡, 브리지, 중복 정리, AI 티 완화 | `ⓐⓐ` | 편집자/작가 승인 또는 테스트 후보본 |
| 삭제 | 중복, 비정본 블록, 반복 정보 제거 | `ⓐ` 또는 `ⓐⓐ` | 정본/정합성/분량 기준 통과 |
| 추가 | 인과 브리지, 지시어 보강, 누락 연결 | `ⓐⓐ` | 없는 설정을 만들지 않는 범위 |

## 적용 전 체크

- `llm-facing/consistency_report.md`에서 편집 가능 항목으로 분리되었는가:
- 회차별 공백 제외 {{minimum_chapter_chars_no_space}}자 기준을 해치지 않는가:
- 중복 회차의 정본 선택이 닫혔는가:
- 문맥형 오타는 앞뒤 문맥 근거가 있는가:
- `changes.json`이 `validate-changes`를 통과했는가:
- 원문형 기호 적용 HWPX를 생성했는가:

## 적용 후 체크

- 후보본 위치:
- diff 위치:
- 기호 적용 HWPX 위치:
- 재평가 대상 회차:
- 새로 생긴 문제:
- 해결된 문제:
- 다음 loop 판단:
